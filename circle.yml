dependencies:
  cache_directories:
    - "~/.stack"
    - ".stack-work"
  pre:
    - curl -L https://www.stackage.org/stack/linux-x86_64-static | tar zx -C /tmp
    - sudo mv /tmp/stack-1.6.3-linux-x86_64-static/stack /usr/bin
  override:
    - stack setup
    - rm -fr $(stack path --dist-dir) $(stack path --local-install-root)
    - stack install hlint
    - stack build
    - stack build --test --no-run-tests

test:
  override:
    - createdb postgres_ws_test
    - stack test
    - git ls-files | grep '\.l\?hs$' | xargs stack exec -- hlint -X QuasiQuotes "$@"
    - cp -f `find .stack-work/install -type f -name postgres-websockets | tail -n1` .
    - gzip ./postgres-websockets
    - cp -f ./postgres-websockets.gz $CIRCLE_ARTIFACTS
